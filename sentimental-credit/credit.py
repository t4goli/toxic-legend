# TODO
from cs50 import get_int

def main():
    length = 1
    pholder = get_num()
    for i in range(1, pholder, *10)
        length += 1
        pholder / 10
    if length % 2 is 1
        plength = length + 1
    else
        plength = length
    for i in range


def get_num():
    while True:
        num = get_int()
        if num > 0:
            return num

